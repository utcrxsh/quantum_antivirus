name: quantum_antivirus
description: A quantum-inspired antivirus solution

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  file_picker: ^6.1.1
  path_provider: ^2.1.1
  intl: ^0.19.0
  provider: ^6.1.1
  flutter_riverpod: ^2.4.9
  dio: ^5.4.0
  shared_preferences: ^2.2.2
  device_info_plus: ^11.4.0
  system_info_plus: ^0.0.6

# Add other dependencies as needed

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/ 
    - assets/logo.png